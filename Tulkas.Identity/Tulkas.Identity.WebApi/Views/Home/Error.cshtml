@model List<string>

@{
    ViewData["Title"] = "Error";
}

<h2>Hata meydana geldi</h2>
<hr />

<div class="alert alert-danger">
    <ul>
        @foreach (var error in Model)
        {
            <li>@error</li>
        }
    </ul>
</div>